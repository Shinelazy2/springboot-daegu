spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://39.120.148.25:1270/rtvi?serverTimezone=Asia/Seoul
    username: root
    password: ej2soft!@!
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

    properties:
      hibernate:
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
